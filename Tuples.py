# Tuples = A list that cannot be modified and defined by ()

numbers = (1, 2, 3)
# Count is used to count the occurrence of an item
numbers.count(2)
print(numbers.count(2))
# Index to find the index of the first occurrence of an item
print(numbers.index(1))
numbers.index(1)
